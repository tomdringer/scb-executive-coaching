<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing Stories</h1>

  <%= turbo_frame_tag "stories" do %>
    <%= render partial: "form", locals: { stories: @stories } %>
  <% end %>

  <%= link_to "Back to Story", stories_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
